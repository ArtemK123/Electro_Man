#ifndef MAP_H
#define MAP_H

/*#include "classes.h"
#include "enemies.h"
#include <QtXml>

class Room {
public:
    int** matrix;
    vector<Texture*> textures;
    vector<Enemy*> units;
    vector<Ammo*> items;

    Room(string path);
    virtual ~Room();
};


class Map {
public:
    int x;
    int y;
    Room* current_room;
    Room* rooms;


    Map();

    Room* getRoom();

    virtual ~Map();
};*/

#endif // MAP_H
