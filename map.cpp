#include "map.h"

/*Room::Room(string path) {
    textures = vector<Texture*>();
    units = vector<Enemy*>();
    items = vector<Ammo*>();
    QXmlStreamReader file(path.c_str());
    while (!file.atEnd()) {

    }


}

Room::~Room() {
    for (auto ptr : textures) {
        delete ptr;
    }
    for (auto ptr : units) {
        delete ptr;
    }
    for (auto ptr : items) {
        delete ptr;
    }
}


Map::Map() {

}

Map::~Map() {
    delete rooms;
}
*/
